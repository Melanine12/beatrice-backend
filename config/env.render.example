# Configuration Render - Production Environment
# Copiez ce fichier vers .env sur votre serveur Render

# Configuration de la base de données (à configurer dans Render)
DB_HOST=your-render-db-host
DB_PORT=3306
DB_NAME=your-render-db-name
DB_USER=your-render-db-user
DB_PASSWORD=your-render-db-password

# Configuration du serveur
PORT=10000
NODE_ENV=production

# Configuration JWT
JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production
JWT_EXPIRES_IN=24h

# Configuration des logs
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Configuration des uploads (pour Cloudinary)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# Configuration de sécurité
CORS_ORIGIN=https://hotelbeatricesys.com
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Configuration de la base de données (pool)
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_ACQUIRE=60000
DB_POOL_IDLE=30000

# Configuration PM2 (optionnel)
PM2_PUBLIC_KEY=your-pm2-public-key
PM2_SECRET_KEY=your-pm2-secret-key

# Configuration des notifications (optionnel)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Configuration Cloudinary (pour les images)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Configuration Socket.IO
SOCKET_CORS_ORIGIN=https://hotelbeatricesys.com

# Configuration Firebase pour les notifications push
# IMPORTANT: Pour obtenir les vraies valeurs, allez dans Firebase Console > Paramètres du projet > Comptes de service
# Générez une nouvelle clé privée et téléchargez le fichier JSON
# Les valeurs ci-dessous sont des exemples - remplacez par les vraies valeurs du fichier JSON téléchargé
FIREBASE_PROJECT_ID=beatricehotel-668f6
FIREBASE_PRIVATE_KEY_ID=your_private_key_id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=your_client_email@beatricehotel-668f6.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your_client_id
